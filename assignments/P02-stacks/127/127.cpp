#include <iostream>
#include <vector>
#include <stack>
using namespace std;

struct Card {
         char val;//card value
         char suit;//card color
};

int main(){
  Card c[52];//Store 52 cards
 vector<stack<Card> >v;//Open a vector inside the stack, put the Card structure inside the stack
string str;
}
 

